import styled from 'styled-components';

import { View } from '../common/View';

export const Stack = styled(View)({
  flex: 'auto',
});

export const NavigationComponent = styled(View)({
  position: 'absolute',
  top: '0px',
  left: '0px',
  height: '100%',
  width: '100%',
});
